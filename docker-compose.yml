version: "3.8"

services:
  recallos:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - recallos-data:/data
    environment:
      - PORT=3001
      - DB_PATH=/data/recallos.db

volumes:
  recallos-data:
